import React from 'react';
import './style.css'

export default function Card(){
    return(
        <div className="card-container">
            <p>Components:</p>
            <p>Makes it easier to keep smaller parts working properly</p>
            <p>You can reuse them, meaning less code to write</p>
        </div>
    );
}